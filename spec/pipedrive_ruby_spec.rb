require 'spec_helper'

describe PipedriveRuby do
  it 'has a version number' do
    expect(PipedriveRuby::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
